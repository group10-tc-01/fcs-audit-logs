apiVersion: v1
kind: Service
metadata:
  name: fcs-audit-logs-service
  namespace: fcs-audit-logs
  labels:
    app.kubernetes.io/name: fcs-audit-logs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fcs-audit-logs
  ports:
    - name: http
      port: 8080
      targetPort: http
